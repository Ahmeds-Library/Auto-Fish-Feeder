# Firebase Files

- `database.rules.json` contains safe MVP Realtime Database rules.
- `database.seed.json` contains placeholder demo data. Replace placeholder UIDs before import.

Phase 1 rules are not fully production pairing rules. Do not use claim-any-device pairing without a trusted server or admin flow.
