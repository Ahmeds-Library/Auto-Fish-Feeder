# Legacy Code

This folder preserves the original static web interface and Arduino sketch before the Phase 1 React/Firebase dashboard migration.

- `web-interface/` is the original jQuery/static Firebase UI.
- `firmware-code/` is the original `code/Code.ino` firmware.

They are retained for reference until Phase 2 firmware migration is complete.
